# Данный файл сделан на Python!



Здесь вы можете изменить программу как вам нравится.

print ("Калькутрон3000 версия 931 АЛЬФА-ВЕРСИЯ")
print ("Сделано _DominatorYT_ на языке програмированния Python")
print ("---------------------------------------------------")
print ("Результат быстродействия можете отправлять на email: kenny971@inbox.ru")
print ("Также можете оставить отзыв по этому же email' у. Так как я новичок в этом деле, не вините меня за то что все еще такое сырое, потом буду делать и интерфейс!")
print ("---------------------------------------------------")
print (" Обновление! Улучшил немного интерфейс, Фразы теперь будут более понятными. ")
# Основные операции калькулятора

what = input ( "Что вычислять будем? (+, -):")

a = float( input("Введите первое число: "))
b = float( input("Введите второе число: "))

if what == "+":
    c = a + b
    print ("Результат вычислений:" + str(c))
elif what == "-":
    c = a - b
    print ("Результат вычислений:" + str(c)) 
else:
    error = ("Ошибка при выполнении операции!")
    print ( error )
# Результат вычислений, а также обратная связь.
    print ("Вы получили ваш результат вычислений, а результат быстродействия можете отправлять на email: kenny971@inbox.ru")
    print ("Также можете оставить отзыв по этому же email' у. Так как я новичок в этом деле, не вините меня за то что все еще такое сырое, потом буду делать и интерфейс!")

input()
